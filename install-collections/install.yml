---
- name: Build Collection from Git repo
  hosts: localhost
  become: true
  tasks:
    - name: Check if exist repo
      stat:
        path: "/tmp/collections/"
      register: collection_dir

    - name: Remove older repo version
      file:
        path: "/tmp/modules/"
        state: absent
      when: collection_dir is succeeded

    - name: Get Git repo
      git:
        repo: "{{ repo }}"
        dest: /tmp/collections/

    - name: Build collection 
      shell: "cd /tmp/modules/{{ collection_name }} | ansible-galaxy collection build | ansible-galaxy collection install *.tar.gz"

    - name: Remove files
      file:
        path: "/tmp/modules/"
        state: absent
